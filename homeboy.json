{
  "auto_cleanup": false,
  "changelog_target": "docs/CHANGELOG.md",
  "extensions": {
    "wordpress": {}
  },
  "extract_command": "tmp=$(mktemp -d) && unzip -q {artifact} -d $tmp && cp -a $tmp/blocks-everywhere/. . && rm -rf $tmp {artifact}",
  "id": "blocks-everywhere",
  "remote_path": "wp-content/plugins/blocks-everywhere",
  "version_targets": [
    {
      "file": "blocks-everywhere.php",
      "pattern": "Version:\\s*([0-9.]+)"
    },
    {
      "file": "blocks-everywhere.php",
      "pattern": "VERSION = '([0-9.]+)'"
    }
  ]
}
